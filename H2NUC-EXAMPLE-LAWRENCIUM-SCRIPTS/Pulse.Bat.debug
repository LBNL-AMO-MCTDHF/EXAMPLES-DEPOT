#!/bin/bash

threads=5
procs=4

export OMP_NUM_THREADS=$threads

mpirun --bysocket -n $procs --bind-to-core --bysocket --cpus-per-proc $threads chmctdhf_diatom.debug Inp=Input.Inp.Pulse |tee Outs/Out.pulse.debug

